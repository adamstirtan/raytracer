<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="RayTracer.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RayTracer.Windows"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="RayTracer.Windows">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Controls -->
        <StackPanel Grid.Column="0" Spacing="8">
            <TextBlock FontSize="18" FontWeight="SemiBold">RayTracer — Renderer</TextBlock>

            <TextBlock>Scene</TextBlock>
            <ComboBox x:Name="SceneCombo" />

            <TextBlock>Resolution (WxH)</TextBlock>
            <StackPanel Orientation="Horizontal" Spacing="6">
                <TextBox x:Name="WidthTextBox" Width="70" Text="400" />
                <TextBox x:Name="HeightTextBox" Width="70" Text="300" />
            </StackPanel>

            <TextBlock>Samples per pixel (1,4,9)</TextBlock>
            <TextBox x:Name="SamplesTextBox" Text="1" />

            <TextBlock>Trace depth</TextBlock>
            <TextBox x:Name="TraceDepthTextBox" Text="3" />

            <StackPanel Orientation="Horizontal" Spacing="8">
                <CheckBox x:Name="EnableReflectionsCheckBox" Content="Enable reflections" IsChecked="True" />
                <CheckBox x:Name="EnableDiffuseCheckBox" Content="Enable diffuse" IsChecked="True" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="8">
                <CheckBox x:Name="EnableSpecularCheckBox" Content="Enable specular" IsChecked="True" />
            </StackPanel>

            <Button x:Name="RenderButton" Content="Render" Click="RenderButton_Click" />

            <TextBlock x:Name="StatusTextBlock" Text="Ready" />
        </StackPanel>

        <!-- Image preview -->
        <ScrollViewer Grid.Column="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Image x:Name="RenderImage" Stretch="None" />
        </ScrollViewer>
    </Grid>
</Window>
